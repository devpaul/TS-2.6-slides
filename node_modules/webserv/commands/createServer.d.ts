import { Handler, HandlerFunction } from '../handlers/Handler';
import WebServer from '../WebServer';
export declare type Middleware = () => Array<Handler | HandlerFunction>;
export interface Config {
    directory?: string;
    middleware?: Middleware;
    port?: string;
    timeout?: number;
}
export default function (config: Config): WebServer;
